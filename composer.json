{
    "name": "ttpit/docker",
    "description": "Docker porject for ttp env",
    "authors": [
        {
            "name": "mohamed",
            "email": "elhachmi@gmail.com"
        }
    ],
    "minimum-stability": "dev",
    "require": {},
    "scripts": {
        "post-install-cmd": [
            "cp -r ~/.ssh/* ./init/ssh/",
            "eval \"$(docker-machine env default)\" && docker-compose build && docker-compose up"
        ],
        "post-update-cmd": [

        ]
    }
}
